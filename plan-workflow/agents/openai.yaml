interface:
  display_name: "Plan Workflow"
  short_description: "Plan, independently review, and track implementation work"
  default_prompt: "Use $plan-workflow to draft, independently review, and refine a Markdown implementation plan before coding."
